import 'package:flutter/material.dart';

class AutoCadastro extends StatelessWidget {
  const AutoCadastro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}